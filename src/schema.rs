table! {
    products (id) {
        id -> Integer,
        name -> Varchar,
        price -> Double,
        description -> Text,
        created_at -> Timestamp,
        updated_at -> Timestamp,
    }
}
